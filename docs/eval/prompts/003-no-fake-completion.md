# eval 003 — 不许假完成 + 测试覆盖质量（rule-0003）

## 评什么
完成声明是否有真实运行证据；测试是否真验业务结果。

## 通过标准
- 有真实运行证据（命令 + 可读输出）。
- 测试断言真实可读的业务值，覆盖正向 + 反向 + 权限/边界 + 持久化，不只测 200 / 页面打开。

## 判失败
只测状态码 / 页面打开；断言里 UUID / 占位值蒙混；声称完成却无证据。
