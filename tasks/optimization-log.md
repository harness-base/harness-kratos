# 优化 / 自进化 log

> harness 自进化闭环的记录。两类来源：
> - `落文档`（①，capture）：Stop hook 机械触发（K 轮 / commit 边界 / 变更增量）→ headless Haiku 复查最近对话，捞"做了决策/学了东西却没写进文档"的，提醒落文档（`scripts/turn-backstop.sh`）。
> - `judgment`（②，自进化审查）：`self-evolution` 规范检查 / `self-optimize` 子 agent 的发现。
>
> 重要条目应**晋升到对应的家**：决策→ADR、踩坑→`lessons`、知识→就近 `AGENTS.md`/规则、用户偏好→memory。本 log 是中转站，不是终点。

---

## 2026-06-26 `judgment`（self-evolution 构建时捞到的缺口 + "都做"处理结果）
> ✅=已修　⏳=待办/立项。

- ✅ [docs] CURRENT_STATUS.md 漂移 → 已同步（规则/ADR/技能/scripts/prds/自进化行 + last_updated）。
- ✅ [skills] skills README 假命令 `make skills-index` → 改 `scripts/skills-index.sh` 头部为 `bash scripts/skills-index.sh`，重生成。
- ✅ [templates] ADR-0001/0003 "受影响 skill"栏 → 已补。
- ✅ [index-system] decisions/features 索引漂移 → 新写 `scripts/index-audit.sh`（双向一致性）进 verify；eval 索引本就由 `verify-eval-materials.sh` 守。
- ✅ [project-onboarding] verify 路由盲区 → 加"路由工程路径可达"检查进 verify（命令真能跑仍由各工程 e2e 负责）。
- ✅ [Codex 对等-部分] `self-optimize` 子 agent → 镜像 `.codex/agents/hc-self-optimize.toml` + 注册 config。
- ✅ [process-coverage-部分] bugfix → 新建 `.agents/skills/bugfix/` skill。
- ✅ [① / 自身] ① 落文档提醒正名拆分（backstop/rule-0011/log/ADR-0005 订正）；老 self-optimize skill 退役；子 agent 对齐为②深审执行器。
- ⏳ [Codex 对等-剩余] Codex 原生 hooks（PreToolUse/Stop 等价）未接——`config.toml` 已 `hooks=true`，但需 Codex hook schema，研究性，立项。硬层(git/CI)已对等兜底。
- ⏳ [process-coverage-剩余] 迁移 / loop-engineering 流程——无具体案例，待真触发时按 plan 设计立项（别凭空造低质 skill）。
- ⏳ [templates-小] eval task-review 三联（candidate/decision/summary）无模板——低优先。

## 2026-06-29 `judgment`（self-evolution 维度=skills：doc-sync 重构方案深审）
> 审对象：spec/plan `docs/superpowers/{specs,plans}/2026-06-29-doc-sync-redesign.md`（方案阶段，未实现）。维度=skills 体系规范 / skills-index / 跨 skill 衔接。

- ⏳ [skills｜gap｜major] gates-hooks.md:13 是 self-evolution **自己的审查不变量**（"doc-sync ⇄ turn-backstop ⇄ 资产类型自洽"，预防层=doc-sync skill 的 checklist）。plan 只说"更新机制/新路径"，但删了 skill 后这条不变量的**主语没了**——预防层从"skill checklist 主动回查"变成"无预防层、只剩 reviewer 检测层"。须改写为新拓扑（数据文件清单是唯一判据源、doc-sync-reviewer 检测、turn-backstop 反馈修），并显式声明"预防层取消是有意为之"。修复入口：`.agents/skills/hc-self-evolution/references/gates-hooks.md:13,59`（重写不变量语义，非仅换路径）。
- ⏳ [skills｜gap｜major] ADR-0006:33-34「受影响 skill」栏记 `doc-sync`=本会话新建、承担**预防层**职责。删 skill 直接推翻这条历史声明的现状有效性。plan 把 ADR-0006 列入"历史不改写"——对，但 ADR-0012 须**显式引用 ADR-0006 并声明"其确立的预防层 skill 本 ADR 删除、职责转 reviewer+数据文件"**，否则两 ADR 现状矛盾、后人读 0006 以为 doc-sync 仍是预防层。修复入口：ADR-0012 related_docs/正文加 0006 衔接（plan T1 只列了 0005/0011）。
- ⏳ [skills｜gap｜minor] 新数据文件 `docs/harness/doc-sync-checklist.md` 若带 frontmatter，会被 `scripts/docs-audit.sh` 强校验 source_files/related_docs 指向存在（它扫所有 docs/**/*.md 带 frontmatter 的）。plan T2 说"带 frontmatter"但没说 frontmatter 里别填悬空引用 / 须过 docs-audit。修复入口：plan T2 注明该文件 frontmatter 引用须 docs-audit 绿。
- ✅ [skills｜impact] skills-index 删 skill 无残留风险：`scripts/skills-index.sh` 按现存目录 glob 生成，删目录后 regen 自动去 doc-sync 行，`--check` 进 make verify 守。plan T4 已覆盖。
- ✅ [skills｜impact] 跨 skill 衔接面已全枚举：除 self-evolution refs（docs.md:74 / gates-hooks.md:13,59）外，无任何 SKILL.md 在"何时用"里把用户导向 doc-sync（grep 确认）；删 skill 不留漏触发空洞。plan 关联项清单与实跑 grep 完全一致。
- ⏳ [skills｜self④｜minor] self-evolution 自身 reference `references/skills.md` 提到"常见任务类型…自检"都该有 skill；doc-sync 降级后"改完查文档"这类不再是 skill 而是 reviewer+数据。skills.md 规范第一条"会触发的流程都有 skill"措辞会与新拓扑微冲（文档同步现在无 skill）。建议 skills.md 补一句"被 reviewer 子 agent + 数据文件承接的不必硬做 skill（见 ADR-0011/0012 准入判据）"。修复入口：`.agents/skills/hc-self-evolution/references/skills.md:7`。

## 2026-06-29 `judgment`（self-evolution / 维度=process-coverage / doc-sync 重构设计稿审查）
审查对象：`docs/superpowers/specs/2026-06-29-doc-sync-redesign.md` + plan（设计稿阶段，未实施）。
- **[blocker] 预防层流程被删、无等价替代 = "改完体检"覆盖收窄为纯事后**：doc-sync skill 同时是预防层（漏之前主动查，SKILL.md:12）+ 数据载体（清单）。重构删 skill 只保留检测层（turn-backstop 派 reviewer，事后捞）。gates-hooks.md:13/59 明文「预防层 doc-sync skill 的 checklist + 检测层 turn-backstop ⇄ 资产类型 三者咬合」——删 skill 后"预防层"消失，计划仅把这两处当路径 rewire，未交代预防层去向。断环：原"改完即查（主动）"无替代入口，全压到 turn-backstop 的机械触发（K=8轮/commit/变更≥10）——这恰是设计稿自陈"几乎不被 invoke / 闭环断"的同一层。修复入口：ADR-0012 须显式声明"预防层退场、检测层兜底"是有意取舍并给依据；或在 dev/git-workflow 收尾步骤挂一句"改完比对 doc-sync-checklist"补回常驻预防入口。
- **[major] "反馈口=Stop hook feedback 当轮修"与现有钩子架构不兼容**：设计稿/plan 称 reviewer 漂移"经 Stop hook 反馈给主 agent 当轮修（warn 级，不硬 block）"。但 turn-backstop 由 stop-check.sh:43 `|| true` 调用、自身永 exit 0 且只 echo stderr；HOOKS.md:45 已明记"exit-0 stderr 不注入上下文、没人看见，已删"。Claude Code Stop hook 要回灌 agent 必须 exit 2（块收尾）或 JSON decision:block——与"warn 不硬 block"矛盾。断环：所谓"当轮反馈"在现架构落不了地，会退化成又一条"写 log/stderr 没人看"。修复入口：plan Task3 须指明反馈具体走哪条通道（exit 2 软拦？还是改走 UserPromptSubmit 注入下一轮？），并加守护测试证"反馈真到达 agent 上下文"而非仅 stderr。
- **[minor] 触发面收窄未评估**：原 skill 是"改完任意类型即可主动查"，新机制只在三条机械阈值命中时跑 reviewer。小改动（改 1 个脚本即收尾、不 commit、变更<10）落在所有阈值之外 = 漂移无人查。修复入口：在 spec 风险节补一句覆盖面分析，或把"commit 边界"作为 doc-drift 的强触发（漂移多发于提交后，设计稿决策4 已点到但未锁死）。
- self-evolution 自身：gates-hooks.md:13/59 + docs.md:74 引用已在 plan rewire 列表（✓）；但这三处当前把 doc-sync 定性为"预防层 skill"，rewire 时须改的是**机制描述**（预防层去向）而非仅路径，否则 reference 与新架构语义漂移。

## 2026-06-29 `judgment`（self-evolution 维度=index-system：doc-sync 重构方案深审）
> 审对象：spec/plan `docs/superpowers/{specs,plans}/2026-06-29-doc-sync-redesign.md`（方案阶段，未实现）。维度=索引体系（dir-index / skills-index / index-audit / docs-audit 指针）。实跑 grep+模拟核，非记忆。

- ⏳ [index-system｜gap｜blocker] 新数据文件落 `docs/harness/`，而 `docs/harness` **在 `dir-index.sh --check` 循环里**（`verify-control-plane.sh:63`）。该生成器扫目录下所有 `*.md` 生成 `docs/harness/README.md` 索引。新增 `doc-sync-checklist.md` 后不 regen → `make verify` 红。**已模拟验证**：放入文件 → `dir-index.sh docs/harness --check` EXIT=1（漂移 `+ doc-sync-checklist.md` 行）；删除还原 → EXIT=0。plan T2 建文件但**未带** `bash scripts/dir-index.sh docs/harness` regen；T5 "若需登记则登记"措辞含糊、且框成 index.yaml 式登记（错把它当结构化账本），未点名 dir-index README。修复入口：plan T2 末加一步 `bash scripts/dir-index.sh docs/harness` 并 `make verify` 索引绿。
- ⏳ [index-system｜gap｜major] 删 `doc-sync/SKILL.md` 会制造**自伤悬空指针**：设计稿 spec frontmatter `related_docs` 列 `../../../.agents/skills/doc-sync/SKILL.md`（spec:10），而 `docs-audit.sh` 强校验 docs/** 带 frontmatter 文件的 related_docs/source_files 指向存在。**已实跑验证**：删 SKILL.md → `docs-audit` 报 `✗ ...specs/2026-06-29-doc-sync-redesign.md → 引用不存在：.../doc-sync/SKILL.md`，EXIT=1；还原 → EXIT=0。plan T5 收尾要求"make docs-audit 全绿"会失败。注：此为本变更新 spec 自指被删物，独立于已记的"新 checklist 文件 frontmatter 别填悬空"那条（skills 维度 line 31，讲的是 checklist 自身）。修复入口：plan 加一步——删 SKILL.md 时同步把 spec related_docs 改指新 checklist（或移除该行），收尾 docs-audit 复核。
- ✅ [index-system｜impact] skills-index 删 skill 无残留：`skills-index.sh` 按现存目录 glob 生成、`--check` 进 verify（`verify-control-plane.sh:41`）；删目录 regen 自动去 doc-sync 行。plan T4 已含 regen。**已跑** `skills-index.sh --check`=绿基线。
- ✅ [index-system｜impact] 新 reviewer 落 `.claude/agents/` 已被索引覆盖：`.claude/agents` 在 dir-index 循环（`verify-control-plane.sh:63`），plan T2 已含 `bash scripts/dir-index.sh .claude/agents` regen。**已跑** `.claude/agents --check`=绿基线。
- ✅ [index-system｜impact] ADR-0012 登记路径健全：`docs/decisions` 由 `index-audit.sh` 双向守（登记↔文件，`verify-control-plane.sh:68`），plan T1 含登记 index.yaml + make verify。
- ✅ [index-system｜impact] docs/README.md 描述 `harness/` 用泛述指针（"验证路由/CI/hooks/接入指南"，:34），非文件枚举 → 加 checklist 不漂，rule-0012 合规。**已读核**。
- ⏳ [index-system｜列表外③｜minor] `.codex/agents/` **既无索引也无 `--check`**（不在 dir-index 循环，`.codex/agents/` 无 README）。本变更要建 `.codex/agents/hc-doc-sync-reviewer.toml`，不会破坏任何检查（无检查可破），但暴露**预存的索引覆盖盲区**：codex 双栈 agent 区无地图、无漂移守。非本变更引入，建议另立项给 `.codex/agents/` 补索引或纳入 dir-index（注意 dir-index 当前只扫 `*.md`，需扩 `.toml`）。

## 20260629T065553Z judgment（self-evolution / 维度=docs / doc-sync 重构深审）
- 维度：docs（文档层健康）
- 范围：doc-sync 重构 spec+plan（删 skill / 清单降 docs/harness/doc-sync-checklist.md / 新 doc-sync-reviewer / turn-backstop 派它+反馈）
- 发现 1（blocker）：新建 `docs/harness/doc-sync-checklist.md` 会触发 `dir-index.sh docs/harness --check` 漂移 → `make verify` 红。断环：plan T2 只 regen `.claude/agents`，漏 `docs/harness`。证据：verify-control-plane.sh:63 循环含 docs/harness；dir-index.sh 扫该目录全部 *.md。修复入口：plan T2 加 `bash scripts/dir-index.sh docs/harness` regen。
- 发现 2（blocker）：删 `.agents/skills/doc-sync/` 后，spec 自身 frontmatter `related_docs: ../../../.agents/skills/doc-sync/SKILL.md` 悬空 → `docs-audit` 红。断环：plan T5 声称 docs-audit 全绿，但从不改 spec 自己的 related_docs。证据：docs-audit.sh 扫 docs/**/*.md frontmatter；当前 spec related_docs 该项 OK，删后 MISS。修复入口：spec frontmatter 把该行改指新 checklist 或 ADR-0012。
- 发现 3（minor）：`CURRENT_STATUS.md:37` 描述 ① 机制为"turn-backstop 每轮落文档提醒 + 写 log"，doc-drift 线改为"派 reviewer + 反馈主 agent 当轮修"后该描述对 doc-drift 子项失真。plan T5 只查"是否提 doc-sync"（grep 确认没提），未覆盖此机制描述。修复入口：plan T5 补一句更新 line 37 的 ① 机制措辞。
- 自身（meta ④）：self-evolution 自己的 `references/gates-hooks.md:13,59` + `references/docs.md:74` 把 doc-sync 当 skill 描述（含 `grep '回查' doc-sync/SKILL.md` 检索命令）。plan T4 已列入 rewire。额外：gates-hooks:13 那条"文档同步机制自洽（doc-sync skill checklist + turn-backstop (f) 项 + 资产类型）三者咬合"的不变量本身要重写为新三元组（checklist 数据文件 + doc-sync-reviewer + turn-backstop 派发），plan T4"机制→新机制"措辞偏浅，需点名这条不变量。

## 2026-06-29 `judgment`（self-evolution / 维度=lessons-memory / doc-sync 重构设计稿审查）
审查对象：`docs/superpowers/{specs,plans}/2026-06-29-doc-sync-redesign.md`（设计稿阶段，未实施）。聚焦三件套：lessons 晋升、optimization-log 闭环、memory。

- **[blocker] 反复出现的"改 X→关联 Y 漂移"已 ≥6 条 lesson 跨 3 天，仍无规则，重构只给又一个 scoped patch、没走 add-rule 收口（rule-0007 失守）**：同型 lesson 计 2026-06-27（检测器自漂×1、CURRENT_STATUS 第3次漂×1）、06-28（删 skill 连带漂移"又复发/第 N 次复发"）、06-29（freehand 漏关联项、单场景补丁、README 没核既有漂移），≥6 条。用户在 lesson(line 23-25)亲口点破"你一直补最近踩的那一种、没治'没意识'的根"，并指定根治方向 = 把"改 X→查 Y 地图主动喂进上下文" **或** "在 AGENTS.md 放一条强制 trace 短指令"——即升规则/常驻入口，不是再补一个场景。doc-sync 重构是好的一步（doc-sync-reviewer 把 diff→checklist 漂移主动喂上下文，部分落地"地图喂上下文"），但**只盖删 skill / 改脚本这一类，没把根升成规则**：spec/plan 全文 grep 无 add-rule、无新规则提案，只引用既有 0009/0012。断环：rule-0007"反复出现晋升成规则"对这条最高频的同型坑从未触发。修复入口：本重构收尾走 `add-rule` 立一条"编辑任意产物前/收尾前必比对关联项（索引/README/镜像/文档）"的 warn 级规则，把 doc-sync-reviewer 作为它的机检执行体之一挂上；或在 ADR-0012 显式声明"本次只治删-skill 子集、关联项总规则另立 add-rule"，别让根又烂在 lessons。

- **[blocker] lessons 晋升流程实证从未跑通：46 条全卡 `opt: seen`，晋升成规则(`opt: rule-00NN`)=0、skip=0**：marker 语义（lessons.md:14 + lessons-promote-check.sh:4）= 无标记未整理 / seen 提醒过待决定 / skip 不升 / rule-00NN 已升。实测标题行 `grep '^## 20' | grep 'opt: rule-00'` 命中 0、`opt: skip` 命中 0、`opt: seen` 命中 46。即：correction-nudge 把"攒够就提醒整理"这步跑通了（46 条都标了 seen），但**整理的下一步——决定升/不升——从来没有真正做过一次**，全部停在"提醒过·待决定"。这正是本维度 reference 警示的"log/晋升 空转：rule-0011 失守"在 lessons 侧的镜像。与本次重构强相关：上面那条反复漂移 lesson 也卡在 seen，没人替它做"升/不升"的决定。修复入口：本轮（或紧随）走一次 `self-evolution`→对 46 条 seen 做一遍裁决（该升的走 add-rule 标 `opt: rule-00NN`、不值得的标 `opt: skip`），把 seen 从"终点"变回"中转"；并考虑给 `lessons-promote-check` 加"seen 长期未裁决"的二级提醒，否则 seen 会变成新的烂库位（只是从无标记挪到 seen）。

- **[major] turn-backstop 的 ①capture 至今 0 条入库，重构把同一层的 doc-drift 改走"反馈口"而非 log，但没回头清算"为什么 ①capture 一条都没产出"**：optimization-log 全文 `落文档`(①capture) 实际条目 = 0（只有 header 说明 + 一条"①自身正名"的 judgment 条）。spec 自陈"turn-backstop doc-drift 捕获产出约等于零"——本维度实测确认：不止 doc-drift，**整个 ① capture 通道从没写进过一条**。重构只把 doc-drift 这一项改道（→反馈口），a~e 捕获(决策/偏好/取舍/踩坑/知识) 明确"不动、仍 Haiku→log"。断环：若 ①capture 通道本身没在产出（可能是触发阈值/transcript 截断/Haiku 入库过滤太严，见 lessons 2026-06-26 那条字节截断坑的同源风险），那 a~e 留在原通道 = 留在一条被实证为"0 产出"的通道里。修复入口：spec/ADR-0012 须补一句"a~e 仍走 log 通道，但该通道实证 0 产出的根因已查/待查"——否则改了 doc-sync 一项、其余四类仍可能静默漏；至少把"① capture 通道 0 产出"立成 deferred backlog 条目，别假设它在工作。

- **[self④｜minor] 本维度 reference `lessons-memory.md` 与现实已轻微漂移，doc-sync 重构是更新它的好时机**：reference line 27/44/51 仍写"optimization-log 仅有表头说明、0 条 ⇒ 闭环尚未跑过"，但现况是 ② judgment 已有多批条目（含本次三个维度），且 lessons 侧的晋升机制(`lessons-promote-check.sh`/correction-nudge step4/marker 体系)在 reference 的"检索/判据"里**完全没提**——审 lessons 健康却不提那套 seen/skip/rule marker，是检索盲区。修复入口：`.agents/skills/hc-self-evolution/references/lessons-memory.md` 补 marker 体系检索（`grep 'opt: rule-00' / 'opt: seen'` 看晋升是否真发生）+ 把"0 条闭环没跑过"的描述更新为"① capture 仍 0、② judgment 已跑、lessons 晋升卡 seen"的现状。

## 2026-06-29 `judgment`（self-evolution / 维度=decisions-context-features / doc-sync 重构方案深审）
> 审对象：spec/plan `docs/superpowers/{specs,plans}/2026-06-29-doc-sync-redesign.md`（方案阶段，doc-sync skill 仍在、reviewer/清单未建）。维度=ADR/context/features 三区真实一致不漂移。实跑 grep/diff/docs-audit 核，不靠记忆。

- ⏳ [decisions｜gap｜blocker] **ADR-0012「受影响 skill」栏会把 self-evolution 误标为「否」=eval-011 老坑复现**。plan T1 写「受影响 skill 栏（doc-sync=是/删；其余逐条否）」，但 plan T4 + spec:53-54 自己要 rewire `self-evolution` 的 3 处 reference（gates-hooks.md:13,59 + docs.md:74）。改了它的 references = 它被实质影响，必须标 **是（已更新）+ 写明改了哪几处**；标「否」就是"做了没记"——正是 spec:64 自陈要防的 eval-011 直接判失败口径（templates/adr.md:16-17 强制此栏、ADR-0004 就栽在这）。修复入口：ADR-0012「受影响的 skill」栏把 self-evolution 列 是、doc-sync 列 是/删、其余（add-rule/dev/git-workflow/prd-elicitation/test-case）列 否。
- ⏳ [decisions｜gap｜major] **ADR-0012 related_docs 漏接 ADR-0006，两 ADR 现状矛盾**。ADR-0006:20/33-34 明文「预防层 = doc-sync skill（本会话新建、承担预防层职责）」。删 skill 直接推翻这条现状有效声明，但 plan T1 的 related_docs 只列 0005/0011。ADR 正文虽不改写（历史），但 ADR-0012 须**显式引用 0006 并声明"其确立的预防层 skill 由本 ADR 删除、职责转 doc-sync-reviewer + 数据文件清单"**，否则后人读 0006 仍以为 doc-sync 是活的预防层。修复入口：ADR-0012 related_docs + 正文加 0006 衔接句。
- ⏳ [context｜gap｜major] **CURRENT_STATUS.md:37 的 ① 机制描述会漂，被 plan 误判为"不动"**。该行写「自进化（① 落文档提醒…）｜turn-backstop.sh（每轮落文档提醒）…」。本次重构正是改 ① 的 doc-drift 路径（写 log → 派 doc-sync-reviewer + 当轮反馈修）。spec:58 / plan:43 以"grep 确认 CURRENT_STATUS 不含 doc-sync 字面 → 无需改"草率排除——连带项不是字面串而是**机制描述**（gates-hooks.md:13/59 自陈的语义漂移面）。修复入口：plan T5 已留口子（"skill 行/路由若提 doc-sync 则改"），但须扩为"CURRENT_STATUS:37 ① 机制描述同步为新拓扑"，否则状态页与新机制漂移。注：line 28 skills 已指针化（"以 README.md 为准"），删 doc-sync 不漂计数——这部分 plan 判对。
- ⏳ [context｜gap｜minor] **新设计稿自身 related_docs 指向将被删的 doc-sync/SKILL.md → make docs-audit 必红**。spec:10 frontmatter `related_docs` 列 `../../../.agents/skills/doc-sync/SKILL.md`；T4 删该文件后，`scripts/docs-audit.sh`（校验所有带 frontmatter 的 docs/**/*.md 的 related_docs 存在）会对设计稿自身报"引用不存在"。plan T5/T6 要求 docs-audit 绿、却没把"改设计稿自身 related_docs"列进关联项。修复入口：T4/T5 增一步——删 skill 后把 spec related_docs 的 doc-sync/SKILL.md 改指新 checklist 或移除。同理 plan 自身若引用 SKILL.md 也要查。
- ✅ [decisions｜impact] 三区 index⇄目录双向一致现状健康：decisions（11 条 ADR-0001~0011）/features（6 条全 done）实跑 diff 无孤儿无幽灵；ADR-0001~0011 均被按号引用（git grep 计数 3~46 次，无悬挂）；无任何文件预引 ADR-0012（无 dangling）。新增 ADR-0012 + index 登记即可，无连带清理。
- ✅ [features｜impact] doc-sync 重构纯控制面 / 文档 / 脚本改动，不动业务代码，rule-0001 不触发 → features 区无需新增需求包、delivery_status 无虚高风险。plan 未误立 feature 包，判对。
- ⏳ [self④｜minor] self-evolution 自身 reference `docs.md:74` / `gates-hooks.md:13,59` 已在 plan rewire 列表（覆盖✓）；但本维度手册 `references/decisions-context-features.md` 的"常见漏洞模式"可补一条本案样本：「删 skill 时受影响栏漏标连带改的 self-evolution（rewire 了它的 refs 却标否）」——与现有 ADR-0004 eval-011 样本同型、可强化判据。修复入口：`.agents/skills/hc-self-evolution/references/decisions-context-features.md` 漏洞模式节。

## 2026-06-29 `judgment`（self-evolution 维度=gates-hooks：doc-sync 重构方案深审）
> 审对象：spec/plan `docs/superpowers/{specs,plans}/2026-06-29-doc-sync-redesign.md`（方案阶段，未实现）。维度=护栏/门禁/触发：turn-backstop 改派 reviewer + 反馈口 / .test.sh mutation 自证 / make verify 关联项闸 / codex⇄claude 对等。

- ⏳ [gates-hooks｜gap｜blocker] **"Stop hook feedback 当轮修"机制与实际 Stop-hook 契约矛盾**。设计核心闭环=漂移"经 Stop hook 反馈通道呈给主 agent（warn 级：当轮修或显式 defer），不再写 log"。但 stop-check.sh:3 写死契约：**exit 2=拦收尾+把 stderr 反馈给 agent；exit 0=放行（stderr 不作为可执行反馈）**。turn-backstop 现在在 stop-check.sh:43 以 `|| true` 调、且整脚本 best-effort 永 exit 0——这条路 stderr 到不了主 agent 当轮上下文。能注入当轮上下文的是 UserPromptSubmit 的 stdout（correction-nudge 模式），不是 Stop。所以"warn 级、非 block、又能当轮送达"在现有两个通道里**不存在**：要么 exit 2（=block，设计明确拒绝）、要么 exit 0（=反馈进不去、退回写 log）。plan/spec 把"Stop hook feedback"当成既有能力引用，但没核它真能注入。修复入口：实现前先验证 Claude Code Stop-hook 是否支持非阻断的 systemMessage/JSON 反馈（hookSpecificOutput.additionalContext）；若不支持，改走 UserPromptSubmit-stdout 通道（下一轮注入，非"当轮"）或诚实降级为 exit-2 软阻断+可 defer。这是闭环成立与否的命门。

- ⏳ [gates-hooks｜gap｜blocker] **turn-backstop.test.sh 必须与删 skill 同 PR 改，否则 make verify 在 Task 4 必红**。实跑证实：删 `.agents/skills/doc-sync/SKILL.md` 后 turn-backstop.test.sh 第 4 用例（grep `^\|.*🔴` 该文件、断言 ≥5 行）立即转红、整套 fail=1（`make verify` 含此测试，见 verify-control-plane.sh:23）。plan T3 写了改 .test.sh、T4 写了删 skill，但二者跨 Task；executing-plans 逐 Task 推进时若 T4 先于 T3 落地（或中途 commit），pre-push/CI 红。修复入口：plan 注明"删 skill 与改 .test.sh 必须原子同 commit"；或把 T3 的 .test.sh 改放到 T4 删 skill 之前/同步。

- ⏳ [gates-hooks｜gap｜major] **mutation 自证设计为"neuter 派 reviewer 段→用例转红"，但 turn-backstop 改派 reviewer 后是否仍 hermetic 存疑**。现 turn-backstop.test.sh 头注明"不调 Haiku、hermetic"；第 4 用例只静态 grep 验"判据接通"，从不真跑 claude -p。plan T3 要加"造一处漂移→反馈里出现该漂移项"+mutation。但若新用例真派 doc-sync-reviewer（=真跑 claude -p / Task），就破了 hermetic（要 key/会话、慢、不确定）。turn-backstop.test.sh 必须保持"不调外部"——所以新用例只能验**静态接线**（脚本里有派 reviewer 的调用 + 指新 checklist 路径 + 反馈分支存在），真"reviewer 能查出漂移"归 plan T6 dogfood，不能塞进 verify 里的单测。plan T3 措辞"造漂移→反馈里出现该项"读着像要端到端跑，需澄清=静态接线断言，不破 hermetic。修复入口：plan T3 明确"单测只验接线/分支 mutation、不真 spawn reviewer；端到端归 dogfood"。

- ⏳ [gates-hooks｜gap｜major] **make verify 缺"doc-sync-reviewer ⇄ turn-backstop ⇄ 新 checklist"接通闸——旧闸守的不变量在新拓扑下无机器兜底**。现 turn-backstop.test.sh#4 是 load-bearing 接通闸（判据源断了就红）。重构后判据源从 skill 表移到 `docs/harness/doc-sync-checklist.md`、消费方从"内联 grep"变"派 reviewer"。新拓扑下若：①清单文件被删/改名、②turn-backstop 不再指它、③doc-sync-reviewer agent 不存在/未注册——三者任一断，漂移检测静默失效，而 make verify **现在没有等价闸**（grep 确认 verify-control-plane.sh / docs-audit.sh 均不引用 doc-sync）。docs-audit 只校 frontmatter 引用存在，不校"清单被 turn-backstop/reviewer 真消费"。修复入口：turn-backstop.test.sh 的 #4 须改写为新接通闸（断言 turn-backstop.sh 引用 `docs/harness/doc-sync-checklist.md` + 派 doc-sync-reviewer + 该 agent 的 .md/.toml/config 注册齐全 + 清单 🔴 行 ≥N），保持"任一环断→红"的 mutation 性质。plan T3 未点明这条接通闸要随判据源迁移整体重写（不是删掉#4 了事）。

- ⏳ [gates-hooks｜gap｜major] **doc-sync-reviewer 是 Claude-only 软层，Codex 侧此条 doc-drift 线裸奔，需在硬层兜底或显式标局限**。reviewer 由 turn-backstop（=Stop hook，settings.json，仅 Claude Code 吃）派；`.codex/agents/hc-doc-sync-reviewer.toml`+config 注册只让 Codex **能手动派**该 agent，但 Codex 无等价 Stop-hook 自动触发（optimization-log 已记"Codex 原生 hooks 未接"⏳）。所以 Codex 跑时：doc-drift 既无 turn-backstop 自动捞、又（删 skill 后）无 doc-sync skill 主动提醒——比现状更裸（现状至少有 skill checklist 数据 + 规则在 AGENTS.md）。按 gates-hooks 不变量"任何只挂 settings.json 的红线 Codex 侧裸奔，须硬层等价物兜底"。doc-drift 非 blocker 级红线（warn），软层 Claude-only 可接受，但**必须像 correction-nudge 那样在 HOOKS.md 显式标 codex 局限**，且确认 rule-0011（决策/知识当轮落文档）本体在根 AGENTS.md（Codex 原生读）兜底语义层。修复入口：ADR-0012/HOOKS.md 写明"doc-drift 自动检测=Claude-only 软层，Codex 靠 make verify 机检半 + 根 AGENTS.md 规则，无自动 reviewer"；别把 reviewer 吹成对等。

- ⏳ [gates-hooks｜gap｜major] **从 hook（headless claude -p）里"派子 agent"无先例、机制未验**。现 turn-backstop 跑的是**扁平** headless `claude -p`（带 HARNESS_TRIAGE=1 从 /tmp 跑、刻意防递归），不是 spawn 子 agent。本仓所有子 agent（self-optimize/code-reviewer/eval）都由**主 agent 经 Task 工具在 skill workflow 里**派，从不从 hook 派。plan T3"派 doc-sync-reviewer（沿用现有 claude -p 模式）"混淆了两件事：headless claude -p ≠ spawn 命名子 agent。要让 headless 进程真"用 doc-sync-reviewer 这个 agent"，得确认 `claude -p` 能否 `--agents`/装载 .claude/agents 并在非交互下走该 agent 的 system prompt——未验证。更简洁的做法可能是 turn-backstop 直接内联 reviewer 的 prompt（把 .claude/agents/hc-doc-sync-reviewer.md 正文当 instr，像现在内联 (a)-(f) 那样），子 agent 文件只供 Codex/主 agent 手动派——但那样"双栈 agent 文件"和"hook 实际用法"就脱节，需在设计里讲清。修复入口：plan T3 先验 `claude -p` 派命名子 agent 的可行性；不可行则改"内联 reviewer prompt"并说明 agent 文件的双重用途（hook 内联取 prompt / 交互手动派）。

- ⏳ [gates-hooks｜gap｜minor] **递归/超时/预算/best-effort 等安全不变量必须在改派 reviewer 后逐条保住，plan 未点名**。现 turn-backstop 的安全靠：HARNESS_TRIAGE=1 递归 guard、/tmp 中性目录跑、perl alarm 超时(60s)、--max-budget-usd 封顶、任何失败 exit 0。若改成"派 reviewer"（尤其若真 spawn 子 agent，子 agent 自己可能再触发 Stop hook→再触发 turn-backstop），递归 guard 是否仍覆盖？预算/超时是否仍包住 reviewer 这一跳？plan T3 只说"沿用现有 claude -p 模式"，没逐条确认这五条不变量在新调用形状下不破。修复入口：plan T3 加一行"保住 turn-backstop 五条安全不变量（递归 guard 覆盖 reviewer 跳 / 超时预算包住 / 失败 exit 0），.test.sh 现有递归&不阻断用例继续绿"。

- ✅ [gates-hooks｜impact] HOOKS.md 同步已在 plan T4 关联项列（:54 描述 turn-backstop 对照 doc-sync/SKILL.md 🔴 行→改新机制/路径）；其 frontmatter source_files 列 turn-backstop.sh 不变（仍存在），docs-audit 不会红。✓ 但需补"反馈口语义"+"codex 局限"两段（见上 major）。
- ✅ [gates-hooks｜impact] stop-check.sh 已预防性加 reviewer/review 整词消歧（git diff 证实 staged 改动 + stop-check.test.sh #9 守），避免新 agent 名 doc-sync-reviewer 撞 `## Review` 收尾段正则——这条做对了，是删 skill 前的好铺垫。✓
- ✅ [gates-hooks｜impact] make verify 机检半（skills/rules/dir/prds index + shim + docs-audit）删 skill 后自动收敛（skills-index --check 守残留），plan 不动这半=对。✓
- ⏳ [gates-hooks｜self④｜minor] gates-hooks.md 本身第 13 条不变量（doc-sync ⇄ turn-backstop ⇄ 资产类型）与检索命令第 59 行（`grep '回查' .agents/skills/doc-sync/SKILL.md`）在删 skill 后断主语/断路径——与 skills 维度已记的 :13,59 重叠，但 gates-hooks 视角要补一点：第 12 条"频率合理 / best-effort exit 0"不变量、第 55-57 条"软当硬用 / codex 等价"判据，在 reviewer 拓扑下都要复核适用性。修复入口：重写 gates-hooks.md:13 时一并把"漏新资产"检索（:59）改指 `docs/harness/doc-sync-checklist.md` 的行、并补"doc-drift 检测=软层 reviewer、codex 无自动触发"的判据。

## 2026-06-29 `judgment`（self-evolution / 维度=subagents / doc-sync 重构设计稿审查）
审查对象：spec/plan `docs/superpowers/{specs,plans}/2026-06-29-doc-sync-redesign.md`（设计稿阶段，未实施——`doc-sync-reviewer` 尚不存在）。聚焦新建 doc-sync-reviewer 双栈 + model=haiku + 只读不 Write。

- **[gap｜major] `model: haiku` 偏离全仓子 agent「免-key/用会话模型」不变量，无判据说明为何这个子 agent 例外**：现存 9 个 `.claude/agents/*.md` **无一**声明 `model:` 字段，frontmatter 一律 name/description/tools，描述都写「用当前会话模型，免 API key」（实跑 `grep -rn model .claude/agents/` 零命中）；Codex 侧只用 `model_reasoning_effort`，**从无 `model =` 锁定具体型号**（`grep -rn '^model ' .codex/` 零命中）。plan T2 给新 reviewer 硬钉 `model: haiku`——这是全仓首例，引入新约定却没在 ADR/spec 说清「为何只此一个锁型号、与免-key 原则关系如何」。turn-backstop 当前是 headless 用 `BACKSTOP_MODEL`（可调常量、有 env 覆写口），而子 agent 若硬写 haiku 则失去 turn-backstop 那套「型号可调 + budget 封顶 + 超时」治理。修复入口：ADR-0012 / spec 明写决策（doc-drift 判断量轻、用 haiku 控本，且这是 hook-headless 派的廉价兜底不是会话子 agent），并在 reviewer 描述里讲清它不是「会话模型免-key」那类；或与 turn-backstop 现有 `BACKSTOP_MODEL` 治理对齐（别在 .md frontmatter 硬钉，由派发方 `claude -p --agent doc-sync-reviewer --model "$MODEL"` 传入）。
- **[gap｜minor] reviewer description 缺「免-key/会话模型 or hook-headless 派」定性，与全仓 9 个同类描述措辞不一致**：现存每个 agent 的 description 末尾都有「用当前会话模型，免 API key」（.md）/「免 API key（用会话模型）」（toml `[agents.x]` description）的统一句式；codex toml 正文末尾还有「与 Claude Code 的 .claude/agents/<name>.md 行为一致（免-key 路径）」收口。plan 未要求 reviewer 补这套定性句。漏了 → README/索引 + codex 注册描述里这个 agent 显得「身份不明」（它到底走会话模型还是 hook-headless haiku？）。修复入口：plan T2 补「.md description + toml description + toml 正文收口」三处都写明定性（建议：被 turn-backstop hook headless 派、用 haiku、只读不改）。
- **[gap｜minor] turn-backstop「派 reviewer」的具体调用形态未在 plan 钉死，存在「派不到」风险**：实跑 `claude -p --help` 确认支持 `--agent <agent>`，故 hook headless 派命名子 agent **可行**；但 plan T3 只说「沿用现有 `claude -p` 模式…派 doc-sync-reviewer」，没写清是 `--agent doc-sync-reviewer`（吃 `.claude/agents/` 定义）还是 `--agents '<inline json>'`。注意现有 turn-backstop 从 `/tmp` 中性目录跑且带 `HARNESS_TRIAGE=1`、**不加载项目钩子**——`--agent` 是否能在那种 `cd /tmp` + 非项目根的 headless 上下文里解析到仓内 `.claude/agents/hc-doc-sync-reviewer.md` 需实证（agent 定义发现依赖 CLAUDE.md/项目根）。若解析不到 = 派空、闭环又断。修复入口：plan T3 钉死调用形态；dogfood（T6）须显式验「reviewer 定义在 headless 中性目录上下文真被加载执行」而不仅验「报出漂移」。
- **[gap｜minor] reviewer 工具集应再砍：doc-drift 判断不需要 Glob**：plan 给 `tools: Read, Glob, Grep, Bash`（照搬 code-reviewer）。但本 reviewer 职责窄——读本轮 diff（`git diff`/`status` 走 Bash）+ 读固定清单 `docs/harness/doc-sync-checklist.md`（Read）+ 按清单 grep 目标文档（Grep）。Glob（找文件名 pattern）在「对照固定清单逐行查」里用不上。按「最小工具范围」不变量（subagents.md 规范第2条），Glob 可去。属 minor（多给工具不致命，但偏离最小集原则）。修复入口：plan T2 `tools: Read, Grep, Bash`。
- **[self④｜major] subagents.md 事实锚点（:35）会被本改动作废，须同步**：reference 第35行白纸黑字「现有**三个** `.claude/agents/`：eval/self-optimize/code-reviewer」——这已先被 prd 编排那批（feature-point-writer 等 6 个）作废、现实是 9 个，本就漂；再加 doc-sync-reviewer = 10 个。且第35行明断「每轮兜底 turn-backstop 跑的是 headless Haiku，**不是** spawn 子 agent」——doc-sync 重构后 turn-backstop **会** headless 派一个命名子 agent，这句话语义直接被推翻（虽仍是 hook 触发=硬、但「不派子 agent」不再成立）。这是 self-evolution 自己维度的 reference 与现实硬漂。修复入口：`.agents/skills/hc-self-evolution/references/subagents.md:35` 重写事实锚点（agent 数量改为「以 .claude/agents/README.md 自动索引为准」不硬编计数，遵 rule-0012；并改写「turn-backstop = headless 跑通用 prompt，doc-drift 那项 headless 派 doc-sync-reviewer 命名子 agent」的新拓扑）。
- **[self④｜minor] subagents.md 软硬错配条 + 常见漏洞「triage 误做成子 agent」需补注「hook headless 也可派命名子 agent」**：现 reference（:11/:42/:49）把「子 agent」一律等同「靠主 agent 记得 spawn=软」、把「hook headless」等同「不派子 agent 的内联 prompt」。doc-sync-reviewer 是反例：**hook 触发（硬）+ headless 派命名子 agent**——既是子 agent 又是硬触发，破了「子 agent ⟺ 软」的二分。reference 须补一句澄清：软硬看的是**触发是否机械**（hook vs 主 agent 记得），不是**执行体是否子 agent**；hook 可以 headless 派命名子 agent（`claude -p --agent`），照样硬。否则后续审查会拿旧二分误判 doc-sync-reviewer「该不该是子 agent」。修复入口：`.agents/skills/hc-self-evolution/references/subagents.md:11,42,49`。
- **[impact] Codex 对等齐套已确认路径**：新增 reviewer 须 `.codex/agents/hc-doc-sync-reviewer.toml`（name/description/`model_reasoning_effort` 低档/developer_instructions）+ `.codex/config.toml` 加 `[agents.doc-sync-reviewer]`（description + config_file）——plan T2 已覆盖（实跑确认现有 9 个 agent 均「toml + config.toml 注册」齐套，模式一致）。注意 codex 侧用 `model_reasoning_effort`（plan 说低档，对——现有全 high，首个低档亦属新约定但与 reasoning_effort 语义自洽，可接受）。无 gap。
- **[impact] dir-index 收口无风险**：`scripts/dir-index.sh` 取 frontmatter 首个 `name:`/`title:` 生成 `.claude/agents/README.md`，`--check` 进 make verify（实跑 exit=0 当前无漂）。新增 reviewer regen 后自动入索引；`model: haiku` 在 `name:` 之后不影响抽取。plan T2 已含 regen。无 gap。
- **[impact｜软硬定性✓] 触发口径=hook 机械派，非主 agent spawn（避开 ADR-0005 决策4 坑）**：lessons.md:44 实证用户已锁定「钩子机械派、不再叠收尾主动 spawn」——即 turn-backstop（hook，硬触发）派 reviewer，不靠主 agent 记得。这符合 subagents.md「必须机械可靠的兜底走 hook，别做成靠记得 spawn 的子 agent」。设计方向正确，无软硬错配 gap（前述 :11/:42/:49 是 reference **措辞**需补，非设计错）。
